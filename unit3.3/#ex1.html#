<!DOCTYPE "html">
<script>
    var user = {
	name: "mr smith",
	profession: "dandy fella",
	age: 100000,
	reset: function(){
	    for(var prop in user) {
		if(prop != "reset"){
		    user[prop] = undefined
		}
	    }
	}
    }


</script>
